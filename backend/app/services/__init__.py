"""Services package: business logic layer.

THIS IS WHERE ALL BUSINESS LOGIC LIVES. It stays intentionally empty until
features are designed. Each service module orchestrates repositories, applies
rules, and returns domain results to routers.

Naming convention: <domain>_service.py (e.g. campaign_service.py).
"""
