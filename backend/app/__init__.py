"""AgencyOS backend package.

Package layout follows the layered architecture:

    app/api/v1/endpoints  ->  app/services  ->  app/repositories  ->  app/models

This module intentionally contains no business logic — it exists to make
the `app` directory a proper Python package.
"""
