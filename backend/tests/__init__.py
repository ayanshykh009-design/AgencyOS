"""Backend tests package.

Suites:
- tests/unit/         — isolated unit tests (services, schemas, helpers)
- tests/integration/  — tests touching the database / external services
- tests/api/          — end-to-end API tests through the HTTP layer

Run with `make test` or `pytest` from backend/.
"""
