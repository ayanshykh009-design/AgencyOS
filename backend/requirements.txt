# AgencyOS backend — runtime dependencies
# Install: pip install -r requirements.txt
# Dev/test tooling lives in requirements-dev.txt (kept out of prod images).

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# --- HTTP / async clients ---
httpx>=0.27

# --- Database ---
# Supabase is Postgres; SQLAlchemy (async) + Alembic power local dev/migrations.
supabase>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
# Redis: shared rate limiting / cache across multiple app instances.
redis[hiredis]>=5.0

# --- Security ---
# PyJWT replaces the unmaintained python-jose; pwdlib(Argon2) replaces passlib.
# cryptography for Fernet/AES-GCM credential encryption.
PyJWT>=2.9
pwdlib[argon2]>=0.2
cryptography>=42
python-multipart>=0.0.9
email-validator>=2.2

# --- API guards ---
slowapi>=0.1.9

# --- Observability ---
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0

# --- LLM providers (app/llm) ---
# openai/anthropic/google SDKs are imported lazily per selected provider, so a
# deployment using only OpenAI-compatible/Ollama/DeepSeek can drop these.
openai>=1.40
anthropic>=0.30
google-genai>=1.75

# --- Utils ---
python-dotenv>=1.0
tenacity>=8.2
