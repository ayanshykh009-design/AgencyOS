# AgencyOS backend — Python dependencies
# Split across "core" (runtime) and "dev" (tooling) groups.
# Install: pip install -r requirements.txt
# Dev-only tools are commented out by default; enable when needed.

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# --- HTTP / async clients ---
httpx>=0.27

# --- Database ---
# Supabase is Postgres; SQLAlchemy (async) + Alembic power local dev/migrations.
supabase>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29

# --- Migrations ---
alembic>=1.13

# --- Security ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.9

# --- Utils ---
python-dotenv>=1.0
tenacity>=8.2

# --- Dev / test tooling ---
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
# mypy>=1.10
