# AgencyOS backend — development / CI tooling
# Install: pip install -r requirements-dev.txt
# NOT installed in production images — keep prod slim.

-r requirements.txt

# --- Testing ---
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
# psycopg2-binary powers the SQL-migration integration tests against a real
# PostgreSQL server (they auto-skip when no server is reachable).
psycopg2-binary>=2.9

# --- Linting / typing ---
ruff>=0.4
mypy>=1.10
