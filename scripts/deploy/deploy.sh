#!/usr/bin/env bash
# Deploy placeholder. Extend with your CD pipeline (docker compose, migrations,
# n8n workflow import, etc.). Never run against production without a review.
set -euo pipefail
echo "Deployment pipeline not configured yet."
echo "See docs/deployment.md for the intended flow."
