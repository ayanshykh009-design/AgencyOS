# System prompts

System/role prompts for orchestrator agents and automation layers.

These define the standing behavior of an agent (tone, guardrails, tool
routing) — separate from the channel templates, which generate content.
