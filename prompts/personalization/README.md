# Personalization prompts

Templates that turn raw research into personalization inputs for the channel
prompts above. The output of these prompts feeds `personalizationNotes`.

Files: one versioned prompt per research source (e.g. `v1.0.0-signal-extraction.md`).
Keep outputs structured (JSON) so downstream prompts can consume them reliably.
