# LinkedIn prompts

Templates for LinkedIn outreach: connection requests and direct messages.

Files: one versioned prompt per channel (e.g. `v1.0.0-connection-request.md`).
Connection requests are limited to ~300 characters — keep them tight.
